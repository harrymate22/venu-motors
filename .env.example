# Copy to .env.local to override for your machine.

# The WooCommerce store that takes payments and receives form leads.
VITE_SHOP_URL=https://shop.venumotors.in

# This website's public address — used to build absolute photo links in
# public/shop-catalog.json (npm run catalog:woo).
VITE_SITE_URL=https://venumotors.in
